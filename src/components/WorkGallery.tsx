const projects = [{type:'Drainage',title:'Stormwater & drainage',detail:'Pits, lines, strip drains and practical site drainage solutions.',className:'project-drain'},{type:'Maintenance',title:'Repairs & replacements',detail:'Neat repair work with access and future servicing considered.',className:'project-maintenance'},{type:'Installation',title:'Bathrooms & fit-offs',detail:'Clean pipework and fixture installation from rough-in to handover.',className:'project-install'}];

export function WorkGallery() { return <section className="section work" id="work"><div className="container"><div className="section-heading"><div><div className="eyebrow">SELECTED WORK</div><div className="gold-rule"/><h2>Built clean. Built serviceable.</h2></div><p>These visual panels are ready to be replaced with real JDC job photos as the project gallery grows.</p></div><div className="work-grid">{projects.map(project => <article className={`project-card ${project.className}`} key={project.title}><div className="project-art" aria-hidden="true"><span/><i/><b/></div><div className="project-copy"><span>{project.type}</span><h3>{project.title}</h3><p>{project.detail}</p></div></article>)}</div></div></section>; }
